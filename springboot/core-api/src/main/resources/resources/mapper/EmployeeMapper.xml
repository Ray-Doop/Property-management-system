<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.modules.system.employee.mapper.EmployeeMapper">

    <!-- 模糊查询所有员工 -->
    <select id="selectAll" resultType="com.example.entity.Employee">
        select * from employee
        <where>
            <if test="nickname != null">nickname like concat('%',#{nickname},'%')</if>
        </where>
    </select>

    <select id="selectById" resultType="com.example.entity.Employee">
        select *
        from employee
        where employee_id = #{id}
    </select>

    <select id="selectPage" resultType="com.example.entity.Employee">
        select * from employee
        <where>
            <if test="nickname != null and nickname != ''">
                and nickname like concat('%', #{nickname}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.example.entity.Employee">
        insert into `employee` (username,password,nickname, phone, permission, specialty, status, last_login_time, avatar_url, register_time)
        values (#{username}, #{password}, #{nickname}, #{phone}, #{permission}, #{specialty}, #{status}, #{lastLoginTime}, #{avatarUrl}, NOW())
    </insert>

    <!-- 动态更新逻辑 -->
    <update id="update" parameterType="com.example.entity.Employee">
        UPDATE employee
        SET
        <trim suffixOverrides=",">
            <if test="nickname != null">nickname = #{nickname},</if>
            <if test="username != null">username = #{username},</if>
            <if test="password != null">password = #{password},</if>
            <if test="permission != null">permission = #{permission},</if>
            <if test="specialty != null">specialty = #{specialty},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="status != null">status = #{status},</if>
            <if test="lastLoginTime != null">last_login_time = #{lastLoginTime},</if>
        </trim>
        WHERE employee_id = #{employeeId}  <!-- 必须传入 id 定位记录 -->
    </update>

    <delete id="del" parameterType="com.example.entity.Employee">
        delete from employee WHERE employee_id=#{id}
    </delete>

    <update id="updatePassword">
        update employee
        set password = #{newPassword}
        where employee_id = #{employeeId}
    </update>
</mapper>
